# RLE 编码(数据压缩)

打印机支持的RLE形式:

- 字节级 RLE
- 位级 RLE4X
- 位级 RLE4D
- 位级 RLE5X
- 位级 RLE5D
- 位级 RLE6X
- 位级 RLE6D

---

**位级 RLE:**

## RLE 变体

- RLE D: 不做处理直接编码当前行。
- RLE X: 将上一行与当前行计算XOR然后编码。

## Run(游程)

数据从左到右(字节高位到低位)扫描一行，按连续**元素**分割。示例：

```
111100111011
(1,4) (0,2) (1,3) (0,1) (1,2)
```

每对括号代表一个**游程**。`(元素, 长度)`

必要时，可以换用更大的RLE算法，或fallback到直接打印字节。

实际应用中，不要编码尾部的代表**白色**的**元素**。这是一种浪费。

注意如果是XOR模式，不要编码连续0的末尾。打印机自己会处理。

## 码本

码本是**索引**和**游程长度**之间的对照表。

```python
# 例子
RLE4码本 = [
  {"索引": 0, "游程长度": 1},
  {"索引": 1, "游程长度": 2},
  {"索引": 2, "游程长度": 3},
  {"索引": 3, "游程长度": 4},
  {"索引": 4, "游程长度": 9},
  {"索引": 5, "游程长度": 14},
  {"索引": 6, "游程长度": 29},
  {"索引": 7, "游程长度": 88},
]
```

如果**码本**里没有所需的**游程长度**？可以在分割**游程**时多切几刀。

## 符号

**游程**可以序列化为比特数组，称为**符号**:

E = 元素(这里是代表黑白颜色的比特)。
i = 代表**码本**中，对应**长度**的**索引序号**。

- RLE4: 4位符号: `E i i i`
- RLE5: 5位符号: `E i i i i`
- RLE6: 6位符号: `E i i i i i`

## 返回值

- **符号数量**。
- **符号**按位(MSB-first)拼接成的字节数组(**位流**)。

---

**字节级 RLE:**

## 符号

同上，但是这次是字节。由于字节可能会跟长度标志撞车，所以：

一个符号可以是:

- 相同字节数 `== 63`: `ff <字节>`
- 一个字节，但是 `字节 <= 0xbf`: `<字节>`
- 一个字节，但是 `字节 >= 0xc1`: `c1 <字节>`
- 相同字节数 `>= 2 && <= 62`: `FF <字节>`

## 返回值

- 字节数组长度。不是符号数量。
- **符号**按字节拼接成的字节数组。
